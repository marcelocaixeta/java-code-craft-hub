# Porta do servidor
server.port=8080

# Arquivo JSON de armazenamento (relativo ao working dir).
# No Docker o working dir é /app, então será /app/courses.json
app.storage.file=courses.json

# Datas como texto ISO (yyyy-MM-dd), não como timestamps numéricos
spring.jackson.serialization.write-dates-as-timestamps=false
